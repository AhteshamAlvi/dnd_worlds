// RegionId union and region metadata.
// Uses the full rulebook region set (head, neck, torso front, torso back,
// groin, plus the six paired limb regions) rather than a simplified list,
// because total SU is the denominator of every density calculation.

export {};
