/*
 * EngineResult<T>, Warning, EngineError.
 *
 * The uniform envelope every engine entry point returns: ok, value, trace,
 * warnings, errors. Errors carry a code, the entity involved, what required
 * the failed condition, and a suggested resolution.
 */

export {};
