/*
 * STUB — turning status into numbers.
 *
 * Conditions and injuries are catalogued as authored text today (see
 * conditions.ts). This is where their effects become the concrete
 * AttributeModifiers, d20 adjustments and advantage/disadvantage flags the
 * combat layer reads, folded together with the permanent modifiers that
 * foundation/attributes/resolution.ts already applies.
 *
 * It waits on the combat layer: there is no point resolving "-2 to all d20s"
 * before anything rolls a d20.
 */

export {};
