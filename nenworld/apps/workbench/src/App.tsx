/*
 * Root component. For the first slice this is a single screen: fixture
 * picker, Aura Output input, region concentration control, body map,
 * region detail, trace tree, raw JSON. No tabs until there is a second
 * workspace to navigate to.
 */

export {};
